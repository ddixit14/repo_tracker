name: Generate Stats

on:
  workflow_dispatch:

env:
  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Generate Stats
        run: |
          chmod +x repo_status.sh
          ./repo_status.sh

      - name: Commit and Push Changes
        run: |
          git config --local user.email "ddixit14@gmail.com"
          git config --local user.name "Deepankar"
          git add results.md
          git commit -m "Update stats"
          git push